overwrite: true
schema: "http://localhost:8000/___graphql"
documents: "src/**/*.tsx"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript-common"
      - "typescript-client"
  ./graphql.schema.json:
    plugins:
      - "introspection"
